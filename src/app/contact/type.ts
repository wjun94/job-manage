export interface Node {
    id: string
    companyId: string
    name: string
    createAt: string
    mark: number    // 备注
    pos: string // 职位
    sex: boolean // 性别
    qq: string
    wechart: string
    email: string
    phone: string   // 手机号码
    fixedNum: string    // 固定电话
}